package com.example.practica3.practicafxml.model;
import java.util.List;

public interface DatabaseOperations {

    /**
     * Esta interfaz define los métodos básicos que tienen que implementar
     * las clases del modelo para conectar con vuestra base de datos.
     * Se ha de implementar en el modelo.
     */
    // Método para insertar un usuario
    //boolean insertarUsuario(String nombre, String pwd);

    // Método para obtener un usuario por nombre
    //User obtenerUsuario(String nombre);

    // Método para actualizar la contraseña de un usuario
    //boolean actualizarPassword(String nombre, String nuevoPwd);

    // Método para eliminar un usuario
    //boolean eliminarUsuario(String nombre);

    // Método para obtener todos los usuarios
    //List<User> obtenerTodosUsuarios();
}

